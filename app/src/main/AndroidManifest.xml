<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.datefire">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DateFire">
        <activity android:name=".ui.PizzaActivity"></activity>
        <activity android:name=".ui.ChipsActivity" />
        <activity android:name=".ui.View.BurgerView" />
        <activity android:name=".ui.BurgerActivity" />
        <activity android:name=".ui.View.CervezaView" />
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.BebidasActivity" />
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>